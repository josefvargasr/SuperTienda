<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.josevargas.mysql3" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Login"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ReadComments" >
        </activity>
        <activity android:name=".Register" />
        <activity
            android:name=".Inventario_usuario"
            android:label="@string/title_activity_inventario_usuario" >
        </activity>
        <activity
            android:name=".User_login"
            android:label="@string/title_activity_user_login" >
        </activity>
        <activity
            android:name=".Datos"
            android:label="@string/title_activity_datos" >
        </activity>
    </application>

</manifest>
